- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.gitconfig: gitconfig
    ~/.zshrc: zshrc
    ~/.emacs.d: prelude/
    ~/.asdf: asdf/
    ~/.oh-my-zsh/themes/dracula.zsh-theme: dracula/dracula.zsh-theme
