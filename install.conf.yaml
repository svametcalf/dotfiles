- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.gitconfig: gitconfig
    ~/.zshrc: zshrc
    ~/.emacs.d: prelude/
    ~/.asdf: asdf/

- shell:
  - [git submodule update --init --recursive, Installing submodules]
